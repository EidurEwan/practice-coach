# Editor / tooling
.claude/
.vscode/
.idea/
.DS_Store
Thumbs.db

# Node (the app itself has no dependencies, but keep this honest)
node_modules/
npm-debug.log*

# Design-tool cache
.impeccable/

# The Expo web export is committed at the root because GitHub Pages serves
# `main` from `/`. Its source is in mobile/.

# Playwright MCP scratch output
.playwright-mcp/
